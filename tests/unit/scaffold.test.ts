import { describe, expect, it } from "vitest";

// Phase 0 sanity check: proves the Vitest unit-test runner is wired up
// correctly. Replace/extend with real business-rule tests starting Phase 3.
describe("Phase 0 scaffold", () => {
  it("runs a unit test", () => {
    expect(1 + 1).toBe(2);
  });
});
